<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Categories List</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #f4f6f9;
            margin: 0;
            padding: 0;
        }

        .container {
            width: 80%;
            margin: 40px auto;
            background: white;
            padding: 25px;
            border-radius: 8px;
            box-shadow: 0 4px 8px rgba(0,0,0,0.1);
        }

        h1 {
            text-align: center;
            color: #333;
            margin-bottom: 25px;
        }

        .actions {
            display: flex;
            justify-content: flex-end;
            margin-bottom: 15px;
        }

        .actions a {
            text-decoration: none;
            background-color: #28a745;
            color: white;
            padding: 10px 18px;
            border-radius: 5px;
            font-size: 14px;
            transition: background-color 0.3s ease;
        }

        .actions a:hover {
            background-color: #218838;
        }

        table {
            width: 100%;
            border-collapse: collapse;
            margin-bottom: 20px;
        }

        th, td {
            text-align: left;
            padding: 12px;
            border-bottom: 1px solid #ddd;
        }

        th {
            background-color: #007bff;
            color: white;
        }

        tr:hover {
            background-color: #f1f1f1;
        }

        .btn-group {
            display: flex;
            gap: 8px;
        }

        .edit-btn, .delete-btn {
            padding: 6px 12px;
            font-size: 13px;
            border: none;
            border-radius: 4px;
            cursor: pointer;
            text-decoration: none;
        }

        .edit-btn {
            background-color: #ffc107;
            color: black;
        }

        .edit-btn:hover {
            background-color: #e0a800;
        }

        .delete-btn {
            background-color: #dc3545;
            color: white;
        }

        .delete-btn:hover {
            background-color: #c82333;
        }

        .pagination {
            text-align: center;
        }

        .pagination a {
            display: inline-block;
            margin: 0 5px;
            padding: 8px 14px;
            color: #007bff;
            border: 1px solid #007bff;
            border-radius: 5px;
            text-decoration: none;
        }

        .pagination a.disabled {
            color: #aaa;
            border-color: #aaa;
            pointer-events: none;
        }

        .page-info {
            margin-top: 10px;
            color: #555;
            text-align: center;
        }
    </style>
</head>
<body>
<div class="container">
    <h1>All Categories</h1>

    <div class="actions">
        <a href="/categories/new">➕ Add New Category</a>
    </div>

    <table>
        <thead>
        <tr>
            <th>Category ID</th>
            <th>Category Name</th>
            <th>Actions</th>
        </tr>
        </thead>
        <tbody>
        <% if (data.length === 0) { %>
            <tr>
                <td colspan="3" style="text-align: center;">No categories found.</td>
            </tr>
        <% } else { %>
            <% data.forEach(category => { %>
                <tr>
                    <td><%= category.category_id %></td>
                    <td><%= category.category_name %></td>
                    <td>
                        <div class="btn-group">
                            <a class="edit-btn" href="/category/<%= category.category_id %>/edit">Edit</a>
                            <form action="/category/<%= category.category_id %>?_method=DELETE" method="POST" onsubmit="return confirm('Are you sure you want to delete this category?');">
                                <button type="submit" class="delete-btn">Delete</button>
                            </form>
                        </div>
                    </td>
                </tr>
            <% }) %>
        <% } %>
        </tbody>
    </table>

    <div class="pagination">
        <a href="/categories?page=<%= page - 1 %>&limit=<%= limit %>" class="<%= page <= 1 ? 'disabled' : '' %>">Previous</a>
        <a href="/categories?page=<%= page + 1 %>&limit=<%= limit %>" class="<%= page >= totalPages ? 'disabled' : '' %>">Next</a>
    </div>

    <div class="page-info">
        Page <%= page %> of <%= totalPages %> | Total Categories: <%= total %>
    </div>
</div>
</body>
</html>
